import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'login',
  templateUrl: './login.html',
  styleUrl: './signup.css',
  imports: [RouterLink],
})
export class Login {}
