import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'login',
  templateUrl: './login.html',
  styleUrl: './login.css',
  imports: [RouterLink],
})
export class Login {}
